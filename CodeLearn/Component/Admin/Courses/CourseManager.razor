@page "/admin/course-manager"
@attribute [Authorize(Roles = "Admin")]
@inject ICourseTypeRepository CourseTypeRepository
@inject ICourseRepository CourseRepository




<MudPaper Class="p-4">

    <MudItem>
        <MudLink Href="/admin/course/add-new" Color="Color.Success" Typo="Typo.h5">Thêm khóa học mới</MudLink>
    </MudItem>
    <br />
    <MudItem lg="12">
        @foreach (var item in _courseTypes)
        {
            <MudTable Hover="true" Items="_courses.Where(x => x.CourseTypeId == item.Id)" Bordered="true" Dense="true">
                <HeaderContent>
                    <MudTh>@item.Name</MudTh>
                    <MudTh></MudTh>
                </HeaderContent>
                <RowTemplate>
                    <MudTd DataLabel="name" Class="text-center">
                        <MudLink Underline="Underline.None" Color="Color.Success"
                                 Href="@($"/course/{context.Id}")">@context.Name</MudLink>
                            </MudTd>
                            <MudTd DataLabel="crud" Class="text-center">
                                <MudButton Variant="Variant.Text">
                                    <MudLink Underline="Underline.None" Href="@($"/admin/course/edit/{context.Id}")">Edit</MudLink>
                                </MudButton>
                            </MudTd>
                        </RowTemplate>
                    </MudTable>
                    <br />
                }

    </MudItem>
</MudPaper>
<br />


@code {

    private List<Course> _courses { get; set; }
    private List<CourseType> _courseTypes { get; set; }

    protected override void OnInitialized()
    {
        _courses = CourseRepository.GetAllCourse();
        _courseTypes = CourseTypeRepository.GetAllCourseType();

    }
}
